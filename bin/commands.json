{
    "help": "Show this output.",
    "up": "Start the containers (use -d to run in background).",
    "php": "Run a PHP command.",
    "artisan": "(art|a) Run an artisan command.",
    "down": "Stop all of the containers.",
    "composer": "Run a composer command.",
    "build": "Builds the container (--no-cache)",
    "migrate": "Migrate database",
    "mfs": "Migrate a fresh database and seed",
    "test": "Run tests via artisan",
    "tinker": "Launch tinker session",
    "shell": "Launch a shell session"
}
